<?xml version="1.0" encoding="utf-8"?>
<!-- Aim cursor: Small bright dot that moves with head tilt -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Outer glow ring -->
    <item>
        <shape android:shape="oval">
            <stroke
                android:width="2dp"
                android:color="#88FF88" />
            <solid android:color="@android:color/transparent" />
            <size
                android:width="24dp"
                android:height="24dp" />
        </shape>
    </item>

    <!-- Inner bright dot -->
    <item
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp">
        <shape android:shape="oval">
            <solid android:color="#00FF00" />
            <size
                android:width="8dp"
                android:height="8dp" />
        </shape>
    </item>

</layer-list>

